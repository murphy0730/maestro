# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
.workbuddy/
.worktrees/
.superpowers/

# ── Virtual environments ──
.venv/
venv/
env/

# ── Environment / secrets ──
# Real env files hold credentials and must never be committed.
# Templates (.env.example) and non-secret dev defaults (frontend/.env.development) are kept.
.env
.env.local
.env.*.local

# ── Runtime logs / generated output ──
*.log
maestro/logs/
logs/

# ── Local runtime data (user conversations / uploaded knowledge, never commit) ──
maestro/data/sessions/
maestro/data/knowledge_uploads/
maestro/data/chroma/
maestro/data/skills/
maestro/data/executions/

# ── OS / editor ──
.DS_Store
.idea/

# PyInstaller freeze artifacts (Plan 2)
maestro/dist/
maestro/build/
