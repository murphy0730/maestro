# cmd.exe 的规范批处理格式是 CRLF；LF-only 的 .bat 在多行 ( ) 块和 goto 标签
# 跳转上会出问题（cmd 执行跳转是按字节偏移 seek 文件的）。强制检出为 CRLF。
*.bat text eol=crlf
*.cmd text eol=crlf

# shell 脚本反向锁定，避免在 Windows 上被 autocrlf 改成 CRLF 后 bash 报错。
*.sh text eol=lf
