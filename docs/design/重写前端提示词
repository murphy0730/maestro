你是一名资深前端架构师、产品设计工程师和端到端测试工程师。请在当前仓库中直接完成 Maestro 前端的全面重写，不要只输出方案或停留在分析阶段。

  ## 核心目标

  以以下设计稿为唯一视觉与交互设计真源：

  docs/design/maestro-design-system-v2.html

  全面重写：

  frontend/

  重写后的前端必须高度还原设计稿，同时保留当前前端已有的大部分真实功能，并通过连接真实后端的端到端测试验证全部主要流程。

  禁止调用、依赖或遵循任何 `superpowers` 或 superpowers 系列 skill。不要生成 `docs/superpowers/` 下的设计文档或计划。

  ## 一、真源优先级

  发生冲突时按以下顺序判断：

  1. `docs/design/maestro-design-system-v2.html`：视觉、布局、主题、交互和动效真源。
  2. 当前后端路由、Schema、运行时代码及最新 API contract：接口与业务行为真源。
  3. 当前前端实现和测试：现有功能清单与边界行为参考。
  4. AGENTS.md 和其他文档：仅在与实际代码一致时采用。

  不要因为旧文档仍描述 planning/scheduling/query 三引擎，就覆盖当前已经落地的统一 Agent Runtime。必须先检查实际代码、类型和接口。

  ## 二、工作方式

  开始前：

  1. 阅读完整设计稿，不能只看截图或局部 CSS。
  2. 检查 `frontend/src`、`frontend/electron`、前端测试、状态管理、API 层、MSW mocks。
  3. 检查当前后端 `/sessions`、`/runs`、`/skills`、`/artifacts` 等实际契约。
  4. 运行 `git status`，保留用户已有修改，不覆盖无关工作。
  5. 建立一份“现有功能 → 新界面入口 → API → 测试场景”矩阵，作为实现检查表。
  6. 记录重写前的测试、lint 和 build 基线；已有失败需要区分是基线问题还是新回归。

  完成检查后直接实施，不要仅提交方案等待确认。只有遇到会显著改变产品行为且无法从代码或设计稿判断的问题时才询问。

  ## 三、“完全重写”的定义

  这不是给现有页面换颜色，而是重新构建前端信息架构、应用框架、页面布局和组件层级。

  允许：

  - 重写 `Workspace`、Layout、TopBar、Composer、RunTrace 等现有界面组件。
  - 新建合理的 feature、layout、UI、store 和 hook。
  - 删除已被替代且确认无引用的旧 UI 代码。
  - 保留已经正确且稳定的 API client、SSE、类型和状态逻辑，再将它们接入新界面。
  - 对过大的组件进行拆分，避免继续把逻辑堆进 `Workspace.tsx`。

  禁止：

  - 用 iframe 直接嵌入设计稿。
  - 把设计稿整页 HTML 原样塞入 React。
  - 只做静态假页面。
  - 使用硬编码演示数据冒充真实功能。
  - 为了让测试通过而绕过 API、审批、SSE 或错误处理。
  - 无关的后端重构。
  - 留下无行为的按钮、假设置项或大量 TODO。

  ## 四、必须实现的界面结构

  严格参考设计稿的 A–I 界面：

  ### 1. 主应用框

  实现：

  - 左侧常驻会话侧栏。
  - 中央对话与 Composer。
  - 右侧运行详情栏。
  - 顶部状态栏。
  - 深空极光、蓝图网格和克制的玻璃分层。
  - Electron macOS 标题栏拖拽区域继续正常工作。

  ### 2. 会话侧栏

  实现：

  - 展开宽度约 264px。
  - 折叠后约 56px 图标轨。
  - 品牌标识。
  - 新建会话。
  - 会话搜索。
  - 历史会话分组。
  - 当前会话选中状态。
  - 会话切换。
  - 如果后端已有接口，接入重命名和删除。
  - 技能管理入口。
  - 主题切换入口。
  - 用户与设置入口。
  - 折叠状态应合理持久化。

  不得伪造后端没有提供的会话摘要字段。如果缺少预览或状态字段，使用真实可获得的数据降级展示。

  ### 3. 中央对话区

  保留并重新设计：

  - 历史消息加载。
  - 用户消息与 Agent 消息的清晰区分。
  - Markdown/GFM 渲染。
  - 流式 token 输出。
  - 流式光标和运行中扫描线。
  - 路径升级提示。
  - 空状态和建议提示。
  - 自动滚动，但不能在用户主动查看历史消息时强制抢回滚动位置。
  - 加载、空、连接失败、运行失败、取消、恢复等状态。
  - 切换会话时防止旧请求覆盖新会话。

  ### 4. Composer

  保留并验证：

  - Enter 发送。
  - Shift+Enter 换行。
  - 中文输入法组合输入时不能误发送。
  - 附件选择、显示、移除、上传及 10MB 限制。
  - 技能选择、多选、取消和清空。
  - 技能导入入口。
  - 当前“专家上下文”或等价运行来源能力。
  - 发送、运行中停止、禁用和错误状态。
  - 已选择附件和技能的可视化反馈。

  根据设计稿增加模式选择器：

  - 自动：cyan
  - 排产：blue
  - 调度：orange
  - 查询：teal

  模式轴不得与执行授权颜色混用。

  先核对 `POST /runs` 当前是否支持 `mode`。如果不支持：

  - 不得静默丢弃用户选择；
  - 优先寻找现有兼容字段或路由机制；
  - 若设计功能必须补充契约，只允许做最小、向后兼容的后端 Schema/路由扩展；
  - 同步补充后端测试、前端类型和 API contract；
  - 不得借此重构 Agent Runtime 或制造引擎。

  ### 5. 运行详情栏

  严格实现三态：

  - 右侧驻留。
  - 隐藏，对话区自动扩展。
  - 全屏，详情铺满工作区。

  栏头和顶栏都要能切换状态。

  继续展示真实运行数据：

  - Run ID 或版本信息。
  - fast/structured/升级路径。
  - 当前状态。
  - 步骤列表及成功、运行、等待、失败状态。
  - SSE 事件或有意义的运行轨迹。
  - waiting_approval、reconciling、waiting_external、recovered。
  - completed、failed、cancelled。
  - 待审批操作、影响摘要和策略原因。
  - 确认与拒绝。
  - 审批中的禁用状态。
  - revision 冲突或请求失败的明确反馈。

  ### 6. 技能功能

  实现设计稿中的技能选择弹层和技能管理界面，接入真实接口：

  - 获取技能列表。
  - 搜索和筛选。
  - 多选技能。
  - 信任/撤销信任。
  - 文件校验。
  - `.md`/`.zip` 导入。
  - 导入进度和错误反馈。
  - 删除已导入技能。
  - 删除、导入等变更后同步刷新 Composer 和管理界面。

  不要把“技能管理”做成只有静态卡片的展示页。

  ### 7. 设置

  实现设计稿中的居中设置模态：

  - 通过侧栏右下角打开。
  - Esc 关闭。
  - 点击遮罩关闭。
  - 点击模态内容不能误关闭。
  - 正确管理和恢复焦点。
  - 主题设置。
  - 减少动效。
  - 默认模式。
  - 运行详情栏默认状态。
  - 已存在的个性化设置应继续可用。

  只有已有真实能力的设置才允许可操作。尚无后端能力的栏目应明确标为暂不可用，不得制作会“保存成功”但实际无效的控件。

  ### 8. Electron

  保留 Electron 壳层能力，并按设计稿实现启动屏。确认：

  - macOS hiddenInset/拖拽区域没有被新布局破坏。
  - Electron 构建入口仍有效。
  - 浏览器模式不会错误显示 Electron 专属 UI。

  ## 五、设计系统要求

  将设计稿的 token 系统真正落入生产代码：

  - 深空为默认主题。
  - 极昼是完整的同级主题，不是简单反色。
  - `themeStore` 持久化主题并驱动 `<html data-theme>`。
  - CSS 变量和 Tailwind 使用语义 token。
  - 组件中禁止散落裸 hex。
  - 模式色和授权色保持正交。
  - 青：沟通和导航。
  - 绿：执行和成功。
  - 琥珀：等待确认。
  - 红：失败和危险。
  - 紫：受控执行路径。
  - 蓝/橙/青绿：排产/调度/查询模式。
  - 极光颜色不能随意进入业务组件。
  - 字体按设计稿采用 Space Grotesk 与 JetBrains Mono，并提供可靠的中文系统字体回退。
  - 中文正文不要使用过重字重或过宽字距。
  - 图标统一使用 Lucide 或同风格线性 SVG，禁止 emoji 充当功能图标。
  - 控件必须有 hover、active、disabled、focus-visible 状态。
  - 正文对比度至少达到 WCAG AA。
  - 状态不能只依赖颜色，必须同时有文字或图标。

  动效要求：

  - 150–250ms 的克制微交互。
  - 流式光标只在生成期间显示。
  - 扫描线只在流式生成期间运行。
  - 待审批使用琥珀脉冲。
  - 路径升级提示有轻量进入动画。
  - 抽屉、弹层和详情栏转换平滑。
  - 完整支持 `prefers-reduced-motion`。
  - 动效只能表达状态，不做无意义装饰。

  ## 六、响应式与可访问性

  设计稿以桌面工作台为主，但必须确保常见窗口尺寸可用：

  - 1440px 和 1280px 应完整呈现三栏布局。
  - 较窄桌面窗口自动折叠会话栏或将详情栏转为覆盖层。
  - 不能出现 Composer 被遮挡、审批按钮不可见或横向滚动失控。
  - 为按钮、输入、弹层、列表和状态区提供正确的 label/ARIA。
  - 所有主要功能可通过键盘操作。
  - 焦点环清晰可见。
  - 弹层需要焦点约束和关闭后的焦点恢复。
  - 使用语义化 HTML。

  ## 七、测试要求

  采用测试驱动方式：先为关键行为补充失败测试，再实现。不要删除或弱化现有测试。

  至少完成：

  ### 自动化测试

  在 `frontend/` 运行并通过：

  - `npm test`
  - `npm run lint`
  - `npm run build`
  - `npm run test:electron`

  为新组件和关键交互补充 Vitest + React Testing Library 测试，覆盖：

  - 主题切换和持久化。
  - 会话创建、切换、搜索、重命名和删除。
  - 会话切换竞态。
  - Composer 键盘行为。
  - 附件限制和移除。
  - 技能选择、信任、导入错误和刷新。
  - SSE 流式渲染、重连、去重、恢复和终态。
  - 取消运行。
  - 审批确认和拒绝。
  - 详情栏三态。
  - 设置模态的 Esc、遮罩和焦点行为。
  - reduced-motion。
  - API 错误的用户反馈。

  如果做了最小后端契约扩展，运行对应后端测试，并确保完整后端测试不回归。

  ### 真实端到端验证

  使用真实后端，不以 MSW 结果代替最终 E2E：

  1. 使用仓库提供的启动方式启动前后端。
  2. 检查 `/health`。
  3. 使用 Playwright 或真实浏览器自动化执行完整流程。
  4. 观察浏览器 console、network、SSE 连接和后端日志。
  5. 不允许存在未处理异常、React warning、失败请求被静默吞掉等问题。

  E2E 至少验证：

  - 首次打开和会话恢复。
  - 新建会话。
  - 会话切换和历史消息加载。
  - 发送普通任务。
  - SSE token 流式输出直到完成。
  - 运行停止。
  - 刷新页面后恢复 active run。
  - 附件上传。
  - 技能获取、选择和随任务提交。
  - 技能信任流程。
  - 技能导入/删除等破坏性测试只能在隔离测试数据目录或临时 Skill 副本上执行，禁止破坏用户真实数据。
  - 触发待审批任务，分别验证确认和拒绝。
  - 运行详情栏驻留、隐藏、全屏及恢复。
  - 深空/极昼切换和刷新后持久化。
  - 设置模态。
  - 会话侧栏展开/折叠。
  - 连接失败、API 失败或运行失败状态。
  - 1280px、1440px 和一个窄窗口布局。
  - Electron 相关入口至少通过自动化测试和构建验证。

  ### 视觉验证

  分别打开：

  - 原始设计稿。
  - 重写后的真实应用。

  在深空和极昼主题下，对以下状态截图并逐项比较：

  - 空状态。
  - 流式运行中。
  - 运行完成。
  - 待审批。
  - 详情栏隐藏。
  - 详情栏全屏。
  - 会话侧栏折叠。
  - 技能弹层。
  - 技能管理。
  - 设置模态。

  重点检查布局比例、间距、字体、颜色、边框、玻璃层级、极光、网格、状态颜色和控件密度。发现明显偏差就继续迭代，不能在第一次可运行后停止。

  ## 八、质量门槛

  最终必须满足：

  - 不是静态原型，而是接入真实数据和真实操作的应用。
  - 主要现有功能没有丢失。
  - 设计稿 A–I 的核心界面均已落地。
  - 无无效按钮、假保存、假状态或演示数据冒充生产数据。
  - 无 TypeScript 错误。
  - 无 ESLint warning。
  - 所有测试通过。
  - 构建通过。
  - 真实 E2E 通过。
  - 浏览器 console 无未处理错误。
  - 深空与极昼均经过截图验证。
  - reduced-motion 可用。
  - 不修改或提交真实 `.env` 凭据。
  - 不覆盖用户无关修改。
  - 不创建 git commit，除非我明确要求。

  ## 九、最终交付说明

  完成后给出简洁但有证据的报告，包括：

  1. 重写了哪些页面、组件和状态结构。
  2. 保留了哪些原有功能。
  3. 新增了哪些设计稿交互。
  4. 是否修改后端契约；若修改，说明必要性和兼容方式。
  5. 自动化测试、lint、build、Electron 测试的实际结果。
  6. 真实 E2E 场景及结果。
  7. 截图或视觉验证产物路径。
  8. 尚存限制或未实现项及具体原因。

  不要用“应该可以”“理论上通过”等表述。所有完成结论都必须基于刚刚运行得到的验证结果。
