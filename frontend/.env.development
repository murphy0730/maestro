# Dev data-layer config.
# API base URL + version prefix. Same-origin default lets MSW intercept.
VITE_API_BASE_URL=/api/v1
# Set to `disabled` to hit a real backend instead of MSW mocks.
VITE_API_MOCKING=enabled
