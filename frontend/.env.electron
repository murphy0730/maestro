# Build config for the packaged Electron app (`vite build --mode electron`).
# Packaged app runs from file:// with no Vite proxy, so it hits the backend
# directly. Backend CORS must allow this (see maestro/main.py).
VITE_API_BASE_URL=http://localhost:8000
VITE_API_MOCKING=disabled
