import { Clock } from 'lucide-react';
import { AgentSettings } from '@/features/runtime/AgentSettings';
interface TopBarProps { session: string; sessionId: string; onSessionChange: (sessionId: string) => void; clock: string }
export function TopBar({ session, sessionId, onSessionChange, clock }: TopBarProps) { return <header className="material-chrome flex h-header flex-none items-center gap-[14px] border-b border-border-subtle px-4"><span className="max-w-[280px] overflow-hidden text-ellipsis whitespace-nowrap text-body-sm font-semibold text-text-primary">{session}</span><span className="ml-2 text-caption text-text-tertiary">统一 Agent</span><span className="flex-1" /><AgentSettings activeSessionId={sessionId} onSessionChange={onSessionChange} /><span className="flex items-center gap-[6px] font-mono text-caption text-text-tertiary"><Clock size={13} />{clock}</span></header>; }
